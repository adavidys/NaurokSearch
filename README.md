# NaurokSearch

### Start a Project

To set up the project, run the following command:

```bash
python3 setup.py
```

To execute the main script, use:

```bash
python3 src/main [args]
```

### Arguments

Use the `--help` flag to display a help message with available arguments and their descriptions:

```bash
python3 src/main --help
```
